«адание 4

ѕрограмма реализует схему многопоточного производства и потреблени€ ресурсов через "склад"

* ≈сть _producerCount_ потоков, производ€щих ресурс.  аждый производитель производит 1 единицу ресурса в _producerTime_ времени


* ѕроизводители помещают ресурсы на склад, размера _storageSize_. Ѕольше, чем _storageSize_ ресурсов, на складе хранить нельз€


* ≈сть _consumerCount_ потоков, потребл€ющих ресурсы.  аждый потребитель забирает со склада 1 единицу ресурса в _consumerTime_ времени


* ≈сли склад пустой, потоки-потребители ждут.  ак только ресурсы по€вились, потоки сразу же начинают их потребл€ть


* ≈сли склад переполнен, потоки-производители ждут, пока потребители не освобод€т место


* _producerCount_, _consumerCount_, _producerTime_, _consumerTime_, _storageSize_ Ц конфигурируютс€ в **.properties-файле**


* –есурс имеет уникальный идентификатор дл€ отслеживани€


* ѕроизводители и потребители так же имеют идентификаторы (пор€дковые номера)


* Ћоггируетс€:
  * врем€, номер и тип потока, id-ресурса, событие (произведен или потреблен)
  * количество ресурсов на складе при доставке/потреблении на склад
  * врем€, номер и тип потока, событие (когда потоки переход€т в режим ожидани€ или возобновл€ют работу)


* ¬ задаче не используетс€ java.concurrency